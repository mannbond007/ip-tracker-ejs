<!DOCTYPE html>
<html>
<head>
  <title>IP Result</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="min-h-screen bg-gradient-to-br from-gray-950 via-gray-900 to-black text-white flex items-center justify-center p-3 sm:p-4">
  <div class="w-full max-w-xl bg-gray-900/80 backdrop-blur-xl rounded-2xl shadow-2xl p-4 sm:p-8 border border-gray-800">

    <!-- Header -->
    <h1 class="text-2xl sm:text-3xl font-extrabold text-center mb-6 sm:mb-8">
      <span class="bg-gradient-to-r from-blue-400 via-purple-500 to-pink-500 bg-clip-text text-transparent">
        📡 IP Information
      </span>
    </h1>

    <!-- Info Card -->
    <div class="bg-gray-800/80 rounded-xl p-4 sm:p-6 shadow-inner divide-y divide-gray-700 text-sm sm:text-base">
      <div class="py-2 sm:py-3 flex flex-col sm:flex-row justify-between sm:items-center gap-1 sm:gap-0">
        <span class="text-gray-300 font-medium">IP Address</span>
        <span class="font-semibold text-base sm:text-lg text-blue-400 break-all"><%= data.query %></span>
      </div>
      <div class="py-2 sm:py-3 flex flex-col sm:flex-row justify-between sm:items-center gap-1 sm:gap-0">
        <span class="text-gray-300 font-medium">Country</span>
        <span class="font-semibold text-base sm:text-lg text-green-400"><%= data.country %></span>
      </div>
      <div class="py-2 sm:py-3 flex flex-col sm:flex-row justify-between sm:items-center gap-1 sm:gap-0">
        <span class="text-gray-300 font-medium">City</span>
        <span class="font-semibold text-base sm:text-lg text-yellow-400"><%= data.city %></span>
      </div>
      <div class="py-2 sm:py-3 flex flex-col sm:flex-row justify-between sm:items-center gap-1 sm:gap-0">
        <span class="text-gray-300 font-medium">ISP</span>
        <span class="font-semibold text-base sm:text-lg text-purple-400"><%= data.isp %></span>
      </div>

      <% if (data.note) { %>
        <div class="pt-3 sm:pt-4 text-center">
          <p class="text-red-500 font-semibold text-sm sm:text-base"><%= data.note %></p>
        </div>
      <% } %>
    </div>

    <!-- Back Button -->
    <div class="mt-6 sm:mt-8 flex justify-center">
      <a href="/" 
         class="w-full sm:w-auto text-center px-6 sm:px-8 py-2.5 sm:py-3 bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl font-semibold shadow-md hover:from-blue-600 hover:to-purple-700 transition text-sm sm:text-base">
        ⬅ Back to Home
      </a>
    </div>

  </div>
</body>
</html>
